#declaration
#Var_name = value
# Variables in Python are not “statically typed”.
# We do not need to declare variables before using them or declare their type. 
# A variable is created the moment we first assign a value to it.
firstVariable = [1,2,3,4,5,6,7,8,9,20]
secondVariable=20
print ( firstVariable[0:12])